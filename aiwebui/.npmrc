# NPM Configuration for aiwebui project
# Suppress audit warnings for development dependencies and moderate issues
audit-level=high

# Only show high and critical vulnerabilities
# This prevents noise from moderate/low severity issues that are often false positives

# Optional: Set registry (uncomment if needed)
# registry=https://registry.npmjs.org/

# Performance optimizations
prefer-offline=true
progress=true

# Lock file settings
package-lock=true